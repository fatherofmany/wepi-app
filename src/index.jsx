import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css'; // Tailwind CSS
import TelePi from './TelePi';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TelePi />
  </React.StrictMode>
);
